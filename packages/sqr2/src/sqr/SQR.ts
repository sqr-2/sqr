import { Palette } from "src/types";

// Count of matrix cells
export type SQRSize = 25 | 81;

export class SQR {
  /**
   * @param {SQRSize} size - Equivalent of data-cells count
   */
  constructor(size: SQRSize, palette: Palette, data: Buffer) {
    return data;
  }
}
